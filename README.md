# Letterology

Official instrument. Letterize a username. Read today's luck. Time a decision.

A letter does not change its nature — only its willingness. That is luck.

Live source: [`ImperialDave/Letterology-Calculator`](https://github.com/ImperialDave/Letterology-Calculator)  
Canon house: [`ImperialDave/letterology`](https://github.com/ImperialDave/letterology)

## Use it

1. Enter the handle you actually use.
2. Read the Path (role / how / where) against today's court.
3. Take the three moves: **do this / wait on / ask**.
4. Letterize an act (`quit`, `ask them`, `ship the launch`) to see if the day will have it.
5. Compare two handles. Write an amount as letters. Flip to Greek if you want the second tongue.

## Doctrine, short

- We letterize **usernames**, not birth names.
- First letter sits the house. Weight sits manner and field.
- Digits in a handle are the Count (1 is A, 0 is the Fool).
- Favorable currents = today's date-house plus its allies. Contrary currents = its enemies.
- Luck is willingness, not fate. The decision is yours.

## Develop

```bash
npm install
npm run dev
```

```bash
npm run typecheck
npm test
npm run build
```

TanStack Start + Vite. Auth is optional; readings are open.

## Railway

If Railway says the GitHub repo is not found, grant the [Railway GitHub app](https://github.com/apps/railway) access to **Letterology-Calculator**, then reconnect `main`.

`railway.toml` already sets `npm run build` and `npm start`.
